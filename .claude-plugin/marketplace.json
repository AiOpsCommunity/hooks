{
  "name": "aiops-hooks",
  "owner": {
    "name": "AiOps Community"
  },
  "plugins": [
    {
      "name": "project-checks",
      "source": "./hooks/project-checks",
      "description": "PostToolUse hook that runs the formatter or linter a project already defines on the file just edited, returning failures so they are fixed in the same turn. Opt-in per project."
    }
  ]
}
