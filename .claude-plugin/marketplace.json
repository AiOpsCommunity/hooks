{
  "name": "aiops-hooks",
  "description": "Claude Code hooks shared by the AiOps Community: guardrails, formatters and session helpers, each installable on its own.",
  "owner": {
    "name": "AiOps Community"
  },
  "plugins": [
    {
      "name": "log-watch",
      "source": "./monitors/log-watch",
      "description": "Monitor that watches the log files you name in your own config and reports only the lines matching your patterns, with rate limiting so a noisy log cannot flood the session."
    }
  ]
}
